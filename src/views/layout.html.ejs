<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <title>Tochova</title>
  <% if (env !== 'development') { %>
    <link rel="stylesheet" href="/assets/styles.css">
    <% } %>
</head>

<body>
  <header>
    <div class="navbar">
      <ul>
        <a href="/"> Home </a>
        <a href="/misc/faq"> FAQ </a>
        <a href="/misc/tips"> Tips </a>        
      <% if (currentUser) { %>
        <a href="<%= profilePath(currentUser) %>"> <%- currentUser.name %> </a>
        <a class="pull-right"> href="<%= destroySessionPath %>"> Logout </a>
      <% } else { %>
        <a class="pull-right" href="<%= newSessionPath %>"> Login </a>
      <% } %>
      </ul>
    </div>

  </header>
  <%- body %>
    <script src="/assets/app.js"></script>
    <script src="/assets/appReact.js"></script>
</body>

</html>
